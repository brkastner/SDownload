v2.0.8 BETA:
- Only show one upgrade dialog when prompted again while a previous is still open.

v2.0.7 BETA:
- Fix the report button

v2.0.6 BETA:
- Minor changes

v2.0.5 BETA:
- Check for updates after downloading a song.
- Increase error dialog size for larger error messages.
- Alert the user if the program didn't download due to another instance running.
- Fix automatic updating.

v2.0.4 BETA:

- Allow the option to check for updates from the context menu.
- Properly mark the application as closed so when checking for a running instance on startup, it doesn't close incorrectly.

v2.0.0 BETA:

- TODO: Create the version 2.0 changelog since 1.4
